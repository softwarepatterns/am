# `@softwarepatterns/am-react-components` Examples

Each subdirectory is a standalone example for one auth-input use-case. Every
example has its own `README.md` and local source file.

## Example folders

- `basic-auth-input/`
- `email-sign-in-field/`
- `username-field/`
- `password-current/`
- `password-new/`
- `override-defaults/`
