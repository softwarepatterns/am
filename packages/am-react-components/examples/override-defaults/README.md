# Override Defaults

Use this example when you want the auth-oriented defaults, but still need to
override one of them explicitly for a specific form.

## Files

- `example.tsx`: caller overrides on `EmailAuthInput`
